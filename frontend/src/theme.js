import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "hsla(211, 100%, 56%, 1)",
    },
    secondary: {
      main: "#ffa500",
    },
  },
  typography: {
    fontFamily: "Roboto, san-serif",
  },
});
